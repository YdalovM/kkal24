/**
 * Тайминги эфемерных подсказок в UI (не бизнес-логика калькулятора).
 *
 * ИИ/разработчик: менять здесь, а не «магические числа» в хуках — так проще
 * согласовать доступность (время на чтение) и поведение при рефакторинге.
 */

/** Сообщение после «Рассчитать» (`recalcMessage`, `aria-live`). Должно хватить на медленное чтение и скринридер. */
export const RECALC_SUCCESS_TOAST_MS = 10_000;

/** Время показа строк вроде «Ссылка скопирована» / «Сохранено в браузере». */
export const EPHEMERAL_FORM_HINT_MS = 2_800;
